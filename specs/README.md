# README
This document serves to explain the structure of the specifications (specs).

The following lists the directory structure of the specs and what each folder contains:
- `application/` The specs for the overall application including 
- `controllers/` The specs for controllers
- `models/` The specs for the model including the database logic and diagrams
- `views/`

